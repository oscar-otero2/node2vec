```mermaid
flowchart TD

	for each node in the current walk
		for offsets in a windowsize and offset
			for NegSamN+1 (defined as 5)
				for Dimensions
					product += vector values
				apply some constraints (gradients)
				for Dimensions
					mod some result vectors
			for Dimensions
				mod some result vectors
	
```
